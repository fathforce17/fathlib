import install from '@stores/modules/setup/install';

const initialState = () => ({})

const setup = {
    namespaced: true,
    modules: {
        install
    },
    state: initialState,
    mutations: {
    },
    actions: {
    },
    getters: {
    }
}

export default setup
